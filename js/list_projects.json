var canvas = document.getElementById("projects");
var ctx = canvas.getContext("2d");

$.ajax({
    url: "projects/",
    success: function(data)
    {
	$(data).find("a:contains(.json)").attr("href", function(p, val) {
	    console.log(val)
	});
    }
});
